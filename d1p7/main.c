#include "d1p7.h"
#include <assert.h>

int main()
{
    assert( power_of_n(2) == 4);
    assert( power_of_n(3) == 8);
   // assert( power_of_n(4) == 16);
   // assert( power_of_n(6) == 64);
    return 0;
}
